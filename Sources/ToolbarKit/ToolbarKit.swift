/// ToolbarKit — a SwiftUI titlebar for macOS that you control: any content,
/// the real window buttons, the pre-glass look.
///
/// Start with `View.titlebar(_:background:separator:content:)`.
public enum ToolbarKit {
    public static let version = "0.1.0"
}
